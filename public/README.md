Public assets for Vite.

Place your site-wide static files here so they are served at the root URL.

Structure suggestion:

- /public/favicon.ico
- /public/images/menu/\*.jpg

Any file placed here is available via `/...` paths (e.g., `/images/menu/starter-1.jpg`).
